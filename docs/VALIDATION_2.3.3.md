# 2.3.3 无 Wi-Fi 使用指引验收记录

日期：2026-09-25。

## 本次改动

- 插件设置底部显示一行灰色数据线提示，点击后展开三步操作和 USB 调试设置路径。
- App 首页最下方增加“无 Wi-Fi 使用方法”，按钮跳转系统开发者选项；尚未启用时先引导进入“关于手机”。
- USB 调试勾号读取系统实际开关，返回 App 后重新检查；无法读取时不显示完成图标。
- README、Android / Chrome 使用文档和数据线连接说明同步更新。未新增系统权限。

## 已完成验证

- Chrome：TypeScript 检查、42 项既有测试、构建全部通过。
- Android：33 项既有单元测试、`lintDebug`、`assembleDebug` 通过。Lint 为 0 错误、1 项已有启动图标警告。
- 在独立 Chromium 页面加载实际构建的面板脚本，使用模拟扩展消息检查排版和折叠交互：244 px 面板内提示为一行，展开显示三步说明，均无横向溢出。该检查不模拟 USB 连接成功。
- 本地构建版本均为 2.3.3，Android versionCode 为 39。APK、扩展 ZIP 和 SHA-256 校验文件已生成；当前 Chrome 使用的 `extension/dist` 已覆盖。

## 手机实测（2026-09-25 补验）

- Windows 已识别小米 15 Pro，但最初 ADB 打开接口返回“拒绝访问”。用户临时关闭 Chrome 插件后，ADB 恢复连接。
- 使用 `adb install -r` 覆盖安装成功，实机包信息为 versionName 2.3.3、versionCode 39；首页保留“取消配对”状态，原配对未清除。
- 滑到首页底部，实机确认“无 Wi-Fi 使用方法”、小米设置路径、数据线连接步骤和浏览器选择手机的说明显示完整。
- 系统 `adb_enabled` 与 `development_settings_enabled` 均为 1，按钮显示“USB 调试已开启”和绿色完成图标。
- 点击按钮实际进入 `com.android.settings/.Settings$DevelopmentSettingsActivity`，页面标题为“开发者选项”；按返回键回到 App，开启状态仍正常显示。
- 检查结束已停止 ADB 服务并释放 USB 接口，提醒用户重新启用插件、刷新网页。
- 实机截图及界面层次记录保存在本地忽略目录 `.tooling/usb-guide-check/`。

## 尚未覆盖的场景

- 本机开发者选项已经开启，未实测首次启用时的“去关于手机”分支。
- 未关闭手机 USB 调试开关，关闭后的图标消失行为未做实机验证。
- 本次实机验收针对新增引导及系统跳转，未重做真实短信或长时间连接测试。
